# AIMAX395TTS - Python依赖列表
# 专为AMD AI MAX 395 + 128GB内存平台优化

# 核心依赖
torch>=2.0.0
transformers>=4.35.0
flask>=3.0.0
numpy>=1.24.0
scipy>=1.10.0

# Qwen3-TTS官方库
qwen-tts>=0.1.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0

# Web服务器
gunicorn>=21.0.0

# 工具库
tqdm>=4.65.0
requests>=2.31.0
python-dotenv>=1.0.0

# 可选：用于GPU加速（如果支持CUDA）
# accelerate>=0.24.0
